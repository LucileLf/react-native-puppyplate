// a hook that retrieves and subscribes to the color scheme of the user's device. It returns either 'light' or 'dark', depending on the system settings or user preference.

export { useColorScheme } from 'react-native';
